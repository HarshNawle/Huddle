import SideBar from "../SideBar";
import MessageSection from "../MessageSection";

const HomePage = () => {
  return (
    <div>
      <SideBar />
      <MessageSection />
    </div>
  );
};

export default HomePage;
